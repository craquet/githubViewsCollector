{
  "name": "githubViewsCollector",
  "version": "1.0.0",
  "description": "Collects GitHub repository traffic data and exposes it as Prometheus metrics",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.5.3"
  },
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  }
}
